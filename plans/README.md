# Implementation Plans

Generated by the improve skill on 2026-06-15. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

These plans target UI performance, startup cost, and interaction fluidity in
`D:\DEV\codex-studio`. The repository had no existing `plans/` directory when
this index was created. The working tree was already dirty with broad unrelated
changes; executors must preserve unrelated work and avoid pack/catalog edits
until the catalog coordination plans at the end.

## Execution order and status

| Plan | Title                                                  | Priority | Effort | Depends on          | Status                     |
| ---- | ------------------------------------------------------ | -------- | ------ | ------------------- | -------------------------- |
| 001  | Bound style preset preview preloading to visible cards | P1       | M      | none                | DONE - verified 2026-06-15 |
| 002  | Make viewport transitions compositor-only              | P1       | S      | none                | DONE - verified 2026-06-15 |
| 003  | Expand style render budget coverage for broad searches | P1       | S      | 001                 | DONE - verified 2026-06-15 |
| 004  | Pause passive diagnostics when the app is hidden       | P2       | M      | none                | DONE - verified 2026-06-15 |
| 005  | Move GSAP compatibility off the startup path           | P2       | L      | 002                 | DONE - verified 2026-06-15 |
| 006  | Split style catalog data before raising its budget     | P3       | M      | catalog-agent-clear | DONE - verified 2026-06-15 |
| 007  | Make style catalog search load incrementally           | P3       | L      | 006                 | DONE - verified 2026-06-15 |
| 008  | Public Library, Job Intake, and Reference Boundaries   | P1       | M      | ADR-0033            | DONE - verified 2026-06-28 |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- Plan 001 should land before plan 003 because the render budget coverage should
  guard the final preload behavior, not the current broad preloader.
- Plan 005 depends on plan 002 because removing costly `filter: blur()` first
  makes the shell transition simpler to convert away from the GSAP-backed
  `motion/react` compatibility layer.
- Plans 006 and 007 intentionally come last. Another agent is working around
  catalog/pack/style-manifest state; do not start those plans until that work is
  paused, merged, or explicitly cleared by the operator.

## Recommended waves

1. Wave A: plans 001, 002, 003. These are the highest-leverage UI smoothness
   changes and avoid the catalog-pack split.
2. Wave B: plans 004, 005. These reduce background churn and startup cost after
   the first wave stabilizes.
3. Wave C: plans 006, 007. Run only after catalog-pack coordination is safe.
4. Wave D: plan 008. Safety boundary pass from the 2026-06-28 improve/debt audit.

## Shared verification closeout

For any implementation wave that changes source code, finish with:

| Purpose             | Command                 | Expected on success        |
| ------------------- | ----------------------- | -------------------------- |
| Unit suite          | `bun run test`          | exit 0, all tests pass     |
| Static check        | `bun run check`         | exit 0, no errors          |
| Production build    | `bun run build`         | exit 0                     |
| Chunk budget        | `bun run ui:chunks`     | exit 0, all budget rows ok |
| Style render budget | `bun run styles:render` | exit 0, `violations=0`     |

If a full gate cannot run, record the exact command, failure, and residual risk
in the plan status update.

## Findings considered and rejected

- Live Core Web Vitals or Chrome DevTools trace: not rejected, but not planned
  here because no Chrome DevTools performance-trace tool was available in the
  advisory session. Re-run a live trace after Wave A or Wave B if the Browser or
  Chrome DevTools plugin is available.
- Gallery virtualization rewrite: not planned now. `ImageGrid` already uses
  lazy images and `contentVisibility: 'auto'`; current evidence points to style
  preloads and startup/transition work first.
