# Architecture workplan — 2026-08-18

Status: implemented  
Language: Spanish  
Mode: execution  
Count: 10  
Source: `/improve-codebase-architecture`  
Plan: `.scratch/planning/2026-08-18-arch-perf-2026-08-17/`

Work on the current dirty tree. Do not revert Styles, port/security, or hygiene changes.

## Order

1. ARC-02 Style thumbnail catalog  
2. ARC-07 Recipe identity without full Recipe Module catalog  
3. ARC-03 Style pack load request  
4. ARC-01 Styles browser owner  
5. ARC-06 Character Lab recipe vs catalog  
6. ARC-04 Command Center composer  
7. ARC-05 Catalog card grid  
8. ARC-08 Studio Settings domains  
9. ARC-09 Header Command Center assembly  
10. ARC-10 Thumbnail projection generator  

## Tickets

### ARC-01. Deepen the Styles browser

Blocked by: ARC-02, ARC-03  
Type: AFK  

`StylesRecipe.tsx` is 141 KB and the built chunk is 78.74 KB of an 80 KB budget. It still owns tab routing, collection resolve, visual state, generation plan, user styles, and the card grid.

Acceptance:

- [ ] The recipe page only collects parameters and calls one browser owner.
- [ ] Browse, select, and user-style edit sit behind that owner.
- [ ] `bun run ui:chunks:verify` keeps `styles-recipe` under 80 KB.
- [ ] Focused Styles tests pass.

### ARC-02. Deepen the Style thumbnail catalog

Blocked by: none  
Type: AFK  

`styleThumbnailCatalog.ts` is a mutable global filled by `Object.assign`. Callers know pack keys, category keys, and landing keys.

Acceptance:

- [ ] Callers use `load`, `get`, and `subscribe`. They do not write the map.
- [ ] Pack ownership stays in the catalog module.
- [ ] `lib/styleThumbnailCatalog.test.ts` covers pack_16 `SP05-001` and subscribe.

### ARC-03. Deepen Style pack load request

Blocked by: ARC-02  
Type: AFK  

Load policy is split across `StylesRecipe`, `styleRuntimePackRequirements.ts`, `useStyleRuntimePacks.ts`, and the landing thumbnail loop.

Acceptance:

- [ ] One module answers which runtime packs and thumbnail packs the current tab needs.
- [ ] Landing, All Categories, and a single pack share that module.
- [ ] Existing `styleRuntimePackRequirements.test.ts` stays green and grows for landing thumbs.

### ARC-04. Deepen the Command Center composer

Blocked by: none  
Type: AFK  

`Toolbar.tsx` is 60 KB. It mixes Codex models, Grok blocks, attachments, ratio, and generate.

Acceptance:

- [ ] Provider-specific chrome is selected from a projection, not copied in the composer.
- [ ] Generate, attachments, and ratio stay on one composer interface.
- [ ] Toolbar tests or the nearest composer test cover Codex and Grok blocked states.

### ARC-05. Deepen the Catalog card grid

Blocked by: none  
Type: AFK  

`ImageGrid.tsx` is 52 KB. It mixes filter, sort, virtualization, card actions, and download.

Acceptance:

- [ ] Presentation policy stays in `imageGridPresentation.ts`.
- [ ] The React surface only binds the view to Catalog Entries.
- [ ] Existing image-grid tests stay green.

### ARC-06. Deepen Character Lab recipe vs catalog

Blocked by: none  
Type: AFK  

`CharacterLabRecipe.tsx` (62 KB) imports the generated catalog and both icon atlases.

Acceptance:

- [ ] The recipe imports a small Character Lab view module, not the raw generated tables.
- [ ] Prompt building stays outside the React surface.
- [ ] Character Lab tests or recipe verify still pass.

### ARC-07. Recipe identity without the full Recipe Module catalog

Blocked by: none  
Type: AFK  

`recipeIdentity.ts` imports `RECIPE_MODULES` only to know valid ids. Styles and other recipes pull that graph.

Acceptance:

- [ ] Identity uses a static id list or shell metadata.
- [ ] `recipeModules.ts` is not imported by `recipeIdentity.ts`.
- [ ] `recipeIdentity.test.ts` passes.

### ARC-08. Deepen Studio Settings domains

Blocked by: none  
Type: AFK  

`StudioSettingsModal.tsx` is 47 KB. `useSettingsSurface.ts` already has domain slots, but the modal still hosts libraries, providers, output sources, and maintenance.

Acceptance:

- [ ] Each settings domain has its own demand-mounted panel.
- [ ] The modal only switches domains.
- [ ] Settings tests stay green.

### ARC-09. Shrink Header Command Center assembly

Blocked by: ARC-04  
Type: AFK  

`HeaderToolbar.tsx` is 33 KB. `useStudioShell.ts` still assembles many header facts even after `buildStudioHeaderToolbarProps`.

Acceptance:

- [ ] The shell passes one Command Center projection.
- [ ] Header does not re-derive provider or workspace policy.
- [ ] Header/shell tests stay green.

### ARC-10. Deepen the thumbnail projection generator

Blocked by: ARC-02  
Type: AFK  

`generate-style-thumbnail-projections.ts` still mixes pack grouping, aliases, landing summaries, and check mode.

Acceptance:

- [ ] Pack ownership, landing summaries, and emit/check are separate functions with tests.
- [ ] `bun run styles:thumbnails:check` passes.
- [ ] pack_16 ownership tests stay green.
