# Performance workplan — 2026-08-18

Status: implemented  
Language: Spanish  
Mode: execution  
Count: 10  
Source: `/improve-codebase-performance`  
Plan: `.scratch/planning/2026-08-18-arch-perf-2026-08-17/`

Protect visible Styles cards, Catalog coverage, Character Lab pixels, motion intent, reduced-motion, and generate behavior. Do not trade quality for a smaller number.

Baseline environment: Windows, Bun 1.3.14, current `dist/` from `bun run ui:chunks` (261 chunks, 10,170.69 KB JS). Repeat the same command after each PERF ticket. For browser paths, use a production preview when Vite cold-start is not the ticket.

## Order

After the matching ARC ticket when listed.

1. PERF-05 Recipe identity import  
2. PERF-02 Vite thumbnail scan  
3. PERF-03 Landing thumbnail stampede  
4. PERF-04 All Categories pack load  
5. PERF-01 Styles route size  
6. PERF-06 Character Lab catalog  
7. PERF-07 Composer chunk  
8. PERF-08 Catalog grid first paint  
9. PERF-09 Thumbnail group chunks  
10. PERF-10 Main index leftover  

## Tickets

### PERF-01. Keep the Styles route under budget with room

Blocked by: ARC-01  
User path: open `#recipe-styles`

Baseline: `StylesRecipe-GZ4xCyxB.js` 78.74 KB / 80 KB.

Acceptance:

- [ ] `styles-recipe` chunk is at most 64 KB, or the budget is split into named child chunks that stay required.
- [ ] Landing, All Categories, and pack_16 still show cards.
- [ ] `bun run ui:chunks:verify` and `bun run styles:render:verify` pass.

### PERF-02. Cut cold Vite thumbnail scan

Blocked by: ARC-02, ARC-10  
User path: `bun run dev:ui` then `#recipe-styles`

Baseline: optimizer scan stayed above 8 minutes in this session.

Acceptance:

- [ ] Cold `vp dev` reaches a listening URL in a recorded time under 60 seconds on this host, or the ticket records why the host blocked that and shows a smaller scan input (fewer static `new URL()` in the first graph).
- [ ] Style card images still resolve.
- [ ] `bun run styles:thumbnails:check` passes.

### PERF-03. Stop landing from loading every thumbnail pack

Blocked by: ARC-03, PERF-02  
User path: Styles landing first paint

Baseline: landing effect loads every `pack_*` thumbnail projection, two at a time.

Acceptance:

- [ ] First landing paint loads only visible family source packs.
- [ ] Other packs load on scroll or hover.
- [ ] Folder covers still appear for the first visible family.

### PERF-04. Stop All Categories from loading all 17 runtime packs at once

Blocked by: ARC-03  
User path: All Categories / All Cards

Baseline: `loadAll: true` for 17 packs; 1,677 planned cards.

Acceptance:

- [ ] First paint loads one or two packs and shows their categories.
- [ ] Remaining packs append without a blank “No styles found” flash.
- [ ] Final planned count remains 1,677 when load completes.

### PERF-05. Keep Recipe Module catalog off identity

Blocked by: ARC-07  
User path: any recipe or Styles visual-state path that calls `recipeIdentity`

Baseline: `recipeIdentity.ts` imports `RECIPE_MODULES` (45 KB source).

Acceptance:

- [ ] The identity module does not import `recipeModules.ts`.
- [ ] Styles and Camera recipe tests that use identity still pass.
- [ ] Chunk or import-graph proof shows `recipeModules` absent from the identity module.

### PERF-06. Demand-load Character Lab generated catalog

Blocked by: ARC-06  
User path: open Character Lab

Baseline: recipe statically imports `characterLabCatalog.generated.ts` (150.2 KB).

Acceptance:

- [ ] Opening another recipe does not load that generated catalog.
- [ ] Character Lab still lists the same modes, actions, and options.
- [ ] Atlas pixel contract stays unchanged.

### PERF-07. Shrink composer work on keystroke

Blocked by: ARC-04  
User path: Home prompt typing

Baseline: `Toolbar.tsx` 60.3 KB owns Codex, Grok, attachments, and generate.

Acceptance:

- [ ] Prompt typing does not rebuild provider option lists.
- [ ] Composer chunk or parent render scope is smaller than the current 60.3 KB source surface, or a measured render count drops.
- [ ] Codex and Grok blocked generate states still show.

### PERF-08. Bound Catalog grid first paint

Blocked by: ARC-05  
User path: Home Catalog grid

Baseline: `ImageGrid.tsx` 52.4 KB mounts filter, sort, virtualization, and actions together.

Acceptance:

- [ ] First paint mounts only the visible window plus existing overscan.
- [ ] Idle cards do not mount secondary actions (current `catalogCardActionSurface` contract).
- [ ] Image-grid tests pass.

### PERF-09. Bound Style thumbnail group chunks

Blocked by: ARC-10, PERF-02  
User path: open a Style pack

Baseline: generated groups are 44.6–84.6 KB of static `new URL()` modules.

Acceptance:

- [ ] The largest thumbnail group module is smaller, or packs split without increasing tiny-chunk count above 100.
- [ ] `ui:chunks` packaging stays `tiny<=100` and `chunks<=500`.
- [ ] pack_05 and pack_16 cards still resolve default images.

### PERF-10. Trim leftover main-index weight

Blocked by: ARC-09, PERF-05  
User path: cold Home

Baseline: `index-CdCvgu60.js` 350.24 KB / 488.28 KB.

Acceptance:

- [ ] Main index is smaller than 350.24 KB on a comparable build.
- [ ] Home still shows Command Center, Catalog, and composer.
- [ ] `ui:chunks:verify` main-index stays required and under budget.
